
<p>
  <strong>Title:</strong>
  <%= @post.title %>
</p>

<p>
  <strong>Content:</strong>
  <%= @post.content %>
</p>


<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
<p>
<%= link_to 'Neuer Kommentar', new_comment_path(post_id: @post.id) %>

</p>


<div id="comments" data-post-id="<%= @post.id %>">

    <%= render @post.comments %>
   
  </div>